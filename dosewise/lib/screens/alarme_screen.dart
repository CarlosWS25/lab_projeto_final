import "package:flutter/material.dart";

class alarme_screen extends StatelessWidget {
  const alarme_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: const Color(0xFFA7C4E2),
      body: Center(
        child: Text(
          "Alarme aqui!",
        ),
      ),
    );
  }
}
